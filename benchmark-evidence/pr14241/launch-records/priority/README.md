# Priority launch provenance

The priority run predates durable external launcher records. No original launch JSON or copied wrapper survived.

The exact accepted parameter values are preserved in:

- `../../raw/priority-metadata/run-metadata.json`
- `../../raw/priority-metadata/matrix-plan.csv`
- `../../validated-report.md`

Execution used the exact scripts in `../../scripts/priority-execution-tooling/` at commit `47bcf317a145dcf04cf76d889da9d580a89142e7`. The complete reconstructed orchestrator invocation in `validated-report.md` is derived directly from immutable run metadata and the approved session record; it is labeled as such rather than presented as a surviving launcher artifact.
